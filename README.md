# TextMining1
NLTK Refresh in Python 2 


This code was a basic refresher for me on the NLTK tools. The code was not written to be efficient, but instead written so I could evaluate each step inividually. Most of these steps could be written into a single function but that would have defeated the purpose.
